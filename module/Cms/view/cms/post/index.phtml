<?php
    $this->headTitle($mt);
?>

<h1 class="page-header"><?= $mt ?></h1>

<p>
    <a href="<?= $this->url('post', ['action' => 'add']) ?>" class="btn btn-primary">
        <i class="fa fa-plus"></i> ADD NEW
    </a>
</p>

<?php if(count($posts) > 0) : ?>
    <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
        <thead>
            <tr>
                <th><input type="checkbox" /></th>
                <th>Title</th>
                <th>Image</th>
                <th>Content</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($posts as $post) : ?>
            <tr>
                <td><input type="checkbox" /></td>
                <td>
                    <?= $this->escapeHtml($post->post_title) ?>
                </td>
                <td></td>
                <td>
                    <?= $this->escapeHtml($post->post_content) ?>
                </td>
                <td class="center">
                    <a href="<?= $this->url('post', ['action' => 'edit', 'id' => $post->id]) ?>" class="btn btn-default btn-xs">
                        <i class="fa fa-edit"></i> Edit
                    </a>

                    <a href="<?= $this->url('post', ['action' => 'delete', 'id' => $post->id]) ?>" class="btn btn-default btn-xs">
                        <i class="fa fa-trash"></i> Delete
                    </a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
<?php else : ?>
    <center>
        <font color="red">
            No posts existed or config file not found!
        </font>
    </center>
<?php endif; ?>    

