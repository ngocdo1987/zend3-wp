<?php
    $this->headTitle($mt);
?>

<h1 class="page-header"><?= $mt ?></h1>

<?php if(count($posts) > 0) : ?>
    <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
        <thead>
            <tr>
                <th>Title</th>
                <th>Image</th>
                <th>Content</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($posts as $post) : ?>
            <tr>
                <td>
                    <?= $this->escapeHtml($post->post_title) ?>
                </td>
                <td></td>
                <td>
                    <?= $this->escapeHtml($post->post_content) ?>
                </td>
                <td class="center">
                    <a href="<?= $this->url('post', ['action' => 'edit', 'id' => $post->id]) ?>" class="btn btn-primary btn-xs">Edit</a> 
                    <a href="<?= $this->url('post', ['action' => 'delete', 'id' => $post->id]) ?>" class="btn btn-danger btn-xs">Delete</a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
<?php else : ?>
    <center>
        <font color="red">
            No posts existed or config file not found!
        </font>
    </center>
<?php endif; ?>    

<a href="<?= $this->url('post', ['action' => 'add']) ?>" class="btn btn-primary">ADD NEW</a>