<?php
// module/Album/view/album/album/index.phtml:

$title = 'List categories';
$this->headTitle($title);
?>
<h1><?= $this->escapeHtml($title) ?></h1>
<p>
    <a href="<?= $this->url('category', ['action' => 'add']) ?>">ADD NEW</a>
</p>

<table class="table">
<tr>
    <th>Name</th>
    <th>Description</th>
    <th>&nbsp;</th>
</tr>
<?php foreach ($categories as $category) : ?>
    <tr>
        <td><?= $this->escapeHtml($category->category_name) ?></td>
        <td><?= $this->escapeHtml($category->category_description) ?></td>
        <td>
            <a href="<?= $this->url('category', ['action' => 'edit', 'id' => $category->id]) ?>">Edit</a>
            <a href="<?= $this->url('category', ['action' => 'delete', 'id' => $category->id]) ?>">Delete</a>
        </td>
    </tr>
<?php endforeach; ?>
</table>