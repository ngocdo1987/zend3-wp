<?php
    $this->headTitle($mt);
?>

<h1 class="page-header"><?= $mt ?></h1>

<p>
    <a href="<?= $this->url('category', ['action' => 'add']) ?>" class="btn btn-primary">
        <i class="fa fa-plus"></i> ADD NEW
    </a>
</p>

<?php if(count($categories) > 0) : ?>
    <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
        <thead>
            <tr>
                <th><input type="checkbox" /></th>
                <th>Name</th>
                <th>Description</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($categories as $category) : ?>
            <tr>
                <td><input type="checkbox" /></td>
                <td>
                    <?= $this->escapeHtml($category->category_name) ?>
                </td>
                <td>
                    <?= $this->escapeHtml($category->category_description) ?>
                </td>
                <td class="center">
                    <a href="<?= $this->url('category', ['action' => 'edit', 'id' => $category->id]) ?>" class="btn btn-default btn-xs">
                        <i class="fa fa-edit"></i> Edit
                    </a> 
                    <a href="<?= $this->url('category', ['action' => 'delete', 'id' => $category->id]) ?>" class="btn btn-default btn-xs">
                        <i class="fa fa-trash"></i> Delete
                    </a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
<?php else : ?>
    <center>
        <font color="red">
            No categories existed or config file not found!
        </font>
    </center>
<?php endif; ?>    